# Prometheus scrape config for Wedding AI.
# Scrapes ai-service /metrics when running via docker-compose.
scrape_configs:
  - job_name: ai-service
    static_configs:
      - targets: ["ai-service:8000"]
    metrics_path: /metrics
    scrape_interval: 15s
