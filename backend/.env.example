# For local development (no Docker). When using Docker Compose, the backend is configured from the project root .env and docker-compose.yml.
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=wedding_ai_development
REDIS_URL=redis://localhost:6379/0
FRONTEND_ORIGINS=http://localhost:5173,http://localhost:8080
AI_SERVICE_URL=http://localhost:8000
AI_SERVICE_TIMEOUT=90
RAILS_ENV=development
