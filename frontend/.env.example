# For local Vite dev. With Docker, the image is built using build args from the root .env (VITE_API_BASE_URL, VITE_DEFAULT_WEDDING_ID).
VITE_API_BASE_URL=/
VITE_API_PROXY_TARGET=http://localhost:3000
VITE_DEFAULT_WEDDING_ID=1
